from fractions import *

from primeutils import *

#acc = set()

#for i in range(2, 10**6 + 1):
    #if i % 100 == 0:
        #print i, int(0.42857 * i), int(0.42859 * i) + 1
    #for j in range(int(0.42857 * i), int(0.42859 * i) +1 ):
        #acc.add(Fraction(j, i))

#acc = sorted(list(acc))
#i = acc.index(Fraction(3,7))
#print i, i-1, i+i, acc[i], acc[i-1], acc[i+1]


f
